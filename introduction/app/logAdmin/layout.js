export const metadata = {
  title: "Facbook",
  description: "Connect By Facebook",
};

export default function AdminLayout({ children }) {
  return (
    <>
    <p>Navbar</p>
    {children}
    </>
  );
}

